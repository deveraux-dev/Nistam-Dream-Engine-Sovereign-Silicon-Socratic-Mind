# Foreman-promoted from a gated sidecar draft (MIGRATION §M2).
[package]
name = "forge-intent-v3"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
bytemuck = { version = "1", features = ["derive"] }

[lints]
workspace = true
