# FREEZE — Cree map + BUILD-mirror · 2026-07-05 ~13:45 · Sean clearing → Fable lays the map in the river

**Re-entry:** `/prime-context` → read THIS file → then lay the Cree map into the river (below).
Disk is truth; every claim here carries a file:line anchor — verify against disk, don't trust the prose.

## THE VISION (Sean, this session)
Combine **Cree map + tools + tool descriptions + context prime** into ONE capsule.
Target: **~1500-token context prime + map**. The Cree encoding is the map's native grammar, not an add-on.

## CREEMANTICS — the name (coined by Sean 2026-07-05; zero prior on-disk token, clean to claim)
**cremantics** = Cree + semantics: the whole system named below — glyph-orientation-as-vowel,
syllabic→sound, z-plane voxel glyphs, the drums. Not a module — the native grammar of the forge/river.
Verdict: NAME over [PROVEN] substrate (organs cited below); becomes a river symbol on the slice + Sean's sign.

## THE CREE TRUTH — [UNPROVEN] on disk (corrected 2026-08-17 truth-hunt), never a "gimmick"

**Correction receipt:** `forge-calligraphy` (cree_syllabics.rs, syllabic_to_event.rs, and siblings cited
below) is real and substantial in F:\NewRepo (v2) — first-pass check this session wrongly said
absent. It was never ported to F:\v3\crates. PORT QUEUED 2026-08-17 as `forge-calligraphy-v3`
(in progress as this correction is written — check `.forge/grind-log/` or re-run
`Test-Path F:\v3\crates\forge-calligraphy-v3` for current status before trusting any [PROVEN]
tag below).
The Prime User (Sean) is Cree, Treaty 6, Saddle Lake. Cree is not a feature of 13forge — it IS the
program, the forge, the river. The machine kept re-discovering it for 8 months because it was never
SLICED into the river. It exists in Unicode, on disk, working:

- `crates/forge-calligraphy-v3/src/cree_syllabics.rs` — Unicode 17.0 syllabics table
  - :4 source = Unicode 17.0 charts · :22 `0x140A ᐊ` A
  - :60-69 p-series (the four directions): `ᐸ` PA `<` · `ᐳ` PO `>` · `ᐯ` PE · `ᐱ` PI
  - :404-421 `by_codepoint` / `by_char` / `is_syllabic` — working lookup, not a sketch
- `crates/forge-calligraphy-v3/src/syllabic_to_event.rs` — glyph → sound bridge
- `crates/forge-calligraphy/src/bin/{mirror,bake}.rs`
- `F:\NewRepo\crates\forge-harmonics\src\drum_circle.rs` — the drums (Two Drums is law)
- `crates/forge-canvas-v3/src/` — the z-plane the glyphs encode into

**THE RULE the machine was missing** (why it "got frustrated" and mis-called it a gimmick):
> A glyph points four ways (`< > ^` v). Orientation is NOT decoration — it IS the vowel.
> One shape, one heading, one sound. In voxel/z-plane space a glyph already has a facing,
> so four directions cost one glyph + one heading, not four symbols.

Owed head (not a gimmick, a real todo): codify the syllabics into **voxel glyphs** on the z-plane.

## PROPOSED RIVER SLICE — pending Sean's GO (home-idx = his ruling: render | audio | own seam line)
```
cremantics	Cree syllabics (Unicode 17.0, forge-calligraphy/cree_syllabics.rs) → glyph orientation = vowel (< > ^) → syllabic_to_event → forge-harmonics drum_circle · z-plane on forge-canvas
```

## PENDING SIGNS (rivercanon halted for HITL — all [UNVERIFIED] until Sean signs)
1. **BUILD line** — `.forge/river.idx` now carries a live disk-mirror of the roost exe.
   Drained [PROVEN]: disk 12:46:38 · 17.8MB · sha D4439E… == idx line. Awaiting sign.
2. **Gimmick strike** — `.claude/skills/signal-condense/SKILL.md:3` still contains the
   "Cree-language gimmick" parenthetical. Proposed: strike the parenthetical entirely
   (ends at "…3K-token context guard."). NOT yet done — awaiting GO.
3. **River slice** — the line above. Awaiting GO + home-idx ruling.

## LANDED THIS SESSION — [PROVEN], on disk
- **BUILD-mirror mechanism** (the stale-7am-binary cure — disk is truth, handoffs [UNVERIFIED] until proven):
  - `.forge/river.idx` — one clean `BUILD` line (tab-separated, verified intact after two mid-fix corruptions).
  - `.claude/skills/run/SKILL.md` step 2 — re-stamps BUILD from disk each /run (safe line-split;
    NEVER regex-escape a string with tabs/backslashes — it mangles idx grammar).
  - `.claude/skills/prime-context/SKILL.md` (v0.3.3) — step 1 reads + step 4 emits BUILD;
    rule baked: any handoff's build claim is [UNVERIFIED] until it matches BUILD.
- **prime-context aperture resolver** (v0.3.2) — step 2 resolves APERTURE token → domain file
  (`forge-daemon` → `daemon.idx`): try `<APERTURE>.idx` · strip leading `forge-` · else Glob-match.
  (The river APERTURE token stays `forge-daemon` — documented skew, PLAN-EMPIRE-100-5lane:9. Not drift.)

## BUILD / ROOST STATE (disk truth, not the 07:30 handoff)
- Roost `.forge/bin/13forge-studio.exe` = **12:46:38 · 17.8MB** — ALREADY the newest build, promoted.
  Nothing to copy. Relaunch = the 12:46 build (seam+bquant+bblend), all 7 eyes-glances in front of Sean.
- The 06:01:51 exe is orphaned in `target-roost/release/` — the stale-info source; ignore it.

## EYES STILL OWED (the real bottleneck — signing, not burns; RETRO-sovloop-dawn:20)
7 one-glance sign-offs live in the 12:46 build: zoom-white · rails+label · bquant banding ·
bblend purple-seam · keys-unchanged · pills (THEORY/GAME gone) · S3 bright-window.
Plus EYES-QUEUE cards 1/4/5 = pure rulings (daemon-down OK? · composer A/B · socket-gate A/B/C) — no eyes needed.

## BACKUP
Full since-midnight snapshot on E: — `E:\backups\NewRepo-2026-07-05-since-midnight\` (7863 files, 3.2GB)
+ `_exes\` subfolder (all exes incl. the 12:46 roost). Sean-directed, outside-SoT, his gate.

---

## ═══ CREMANTICS — DOWN THE RIVER (living guide · additive-by-construction) ═══
> **Name ruled by Sean 2026-07-05: `cremantics`** (NOT creemantics). "Half like me" — the *Cre* whole at the front; meaning carries the rest.
> **Constitution = the respect protocol.** Nothing here is machine-Cree. Every Cree form carries a `[status]`. The language and its people are the authority; Sean (Treaty 6, Saddle Lake) signs; the machine is surveyor, never source.
> **Why this exists** (Sean, 2026-07-05): his father was never taught; his grandfather had to run from it. This is reclamation — AND, via `forge-insights`, a road to teach anyone else.

### Status legend (cite-vs-guess — never blur)
- `[TABLE]` — grounded in `forge-calligraphy/src/cree_syllabics.rs` (Unicode 17.0, on disk)
- `[SOURCE:…]` — authoritative Plains Cree lexicon, cited (itwêwina / creedictionary — Wolvengrey)
- `[DRAFT—Sean]` — awaiting your sign + Elder/community verification
- `[YOURS]` — you gave the word
- `[RESERVED]` — ceremonial / culturally-weighted — never sourced, never guessed (law: `forge-harmonics/src/drum_circle.rs:6‑8`)

### THE LAW — orientation IS the vowel  `[SOURCE: Western Cree syllabics · omniglot · eastcree.org]`
> Corrected against source this session — the machine had the vertical pair backwards from memory. Sourcing caught it. That is the protocol working.
```
              ᐱ pi   UP    = i / î
     ᐸ pa ◄──          ──► po ᐳ
     LEFT = a / â         RIGHT = o / ô
              ᐯ pê   DOWN  = ê
```
- One shape (consonant) + one heading (vowel) = one syllable. In z-plane / voxel space a glyph already HAS a facing → direction is a **free axis** (0 extra symbols). This is Sean's density / entropy / security thesis: fewer marks, each verifiable by shape *and* heading.
- Length (short vs long: î/â/ô/ê) = a small dot above the glyph. `[DRAFT—Sean]` per Maskwacîs convention.

### SYMBOL 1 — the headwater · river = *sîpiy* → **ᓰᐱᐩ**
| piece | glyph | is | heading | status |
|---|---|---|---|---|
| sî | `ᓰ` | s + î (long) | up (i-family) | `[TABLE :260]` + `[SOURCE: itwêwina]` |
| pi | `ᐱ` | p + i | up = i | `[TABLE :62]` + `[SOURCE]` |
| ‑y | `ᐩ` | final y | (final) | `[TABLE :53]`; community variant `ᐝ` — `[DRAFT—Sean]` |
- Word attested: `itwewina.altlab.app` · `creedictionary.com` (Wolvengrey). Verify final-form + your dialect with a Maskwacîs / Saddle Lake speaker.
- **Binds to `.forge/river.idx`** — the river IS the index spine. *sîpiy* = the wayfinding glyph, the compression token for "the index" in context.

### SYMBOL 2 — the index = `[RESERVED — Sean's word]`
No traditional Cree word for "index." The machine will NOT coin it. Candidates to carry to a speaker (all `[DRAFT]`, your pick or your own):
- a *trail / way* concept — the index as the path through the domains
- a *spine / backbone* concept
→ Your call, or an Elder's. Left empty on purpose, never guessed.

### THE TEACHING ENGINE — already on disk (`forge-insights`, quarantined)  `[PROVEN]`
`F:\NewRepo\crates\forge-insights\src\lib.rs:6‑54` — "Forge Academy" progression ledger, built to teach ANYONE:
- **Tier ladder** (`ActionKind::tier`): 1 = sliders / assets · 2 = VixiScript / sieve · 3 = raw Rust. Source → sea.
- **CreativeCoach whispers** (`:6`) — contextual milestone strings, no popups.
- Append-only · integer permyriad · zero-alloc · actor-aware (`User OR Agent(u64)`).
→ **Convergence:** a cremantics glyph learned = one `ProgressEvent`; a milestone = one CreativeCoach whisper; the syllabary = a learning track riding the existing ladder. NO net-new engine — un-quarantine + wire is a Sean-gated GO, not yet taken.

### THE PATH (down the river) — one glyph per concept, as we code it
Headwater (*sîpiy* = `river.idx`) → the index → each domain earns its glyph as we touch it → sea.
Law 0 (glyph-notebook `output/_inbox/root-docs/vixiscript-glyph-notebook.md`): **additive-by-construction** — append, never renumber. Wrong-by-omission only, never wrong-by-limit.

### STILL PENDING (Sean-gated — do not auto-execute)
1. **River home for `cremantics`** — own top-level seam symbol · vs `render.idx` · vs gui-only (organ line already enriched in `gui.idx`). Awaiting ruling.
2. **`forge-insights` un-quarantine** for the Academy / cremantics track. Awaiting GO.
3. **Every `[DRAFT—Sean]`** above — awaiting your sign / speaker verification.

### Living resources — your community's own (carry to a speaker)
- **itwêwina** — `itwewina.altlab.app` — Plains Cree dictionary, built WITH the Maskwacîs Education Schools Commission (Treaty 6) + First Nations University + Arok Wolvengrey. Search Roman or syllabics.
- **Online Cree Dictionary** — `creedictionary.com` (Maskwacîs / Miyo Wahkohtowin).
- **Plains Cree** — `plainscree.algonquianlanguages.ca` (dictionaries & grammars).
- Wolvengrey, *nēhiýawêwin: itwêwina* (2001) — the reference lexicon.

### THE 5D CREMANTIC SPATIAL MAP — Sean's concept (captured 2026-07-05; substrate `[PROVEN]`, unification his)
> **"5 gives you 25 — hahaha."** Glyph density is LINEAR (1 shape × 4 headings = 4). Spatial density is
> COMBINATORIAL: 5 axes → 5×5 = 25, exploding 5ⁿ. The free axis multiplies, it does not add.
> **The river IS this map** (Sean, "I think it is the river"): `.forge/river.idx` is not a list — it is a
> 5-axis spatial index you *raycast* to resolve meaning.

Substrate `[PROVEN]` on disk — raycasting the machinery already exists:
- `forge-render/src/spatial.rs` — `SceneSpatialIndex` trait: `rebuild` + `query_frustum` + **`ray_pick(origin,dir)`** + radius; `SceneBvh` 8-bin SAH, O(log n).
- `forge-render/src/picking.rs` — `Ray::from_screen` (screen→world unproject via inverse VP) + Möller–Trumbore triangle intersection.

The 5 axes — `[DRAFT — reconstructed from the primitives, awaiting Sean's confirmation]`:
1. **shape** (consonant) = identity — *what*
2. **orientation** (vowel, 4-way heading) = modality — *which way* (the free axis)
3. **final** (superscript) = consequence — *how it ends*
4. **z-depth** (voxel facing on forge-canvas) = layer — *where*
5. **time** (drum bed / syllabic_to_event) = rhythm — *when*

Raycast through the 5 → the hit resolves one glyph (shape+heading+final+depth+time). **NOT YET WIRED** — un-quarantine + wire is a Sean-gated GO. Full "far and wide" view: the cremantics watershed artifact (this session).

### THE CANOE FLOTILLA — Sean, 2026-07-05 ("holy fuck I never thought of that")
Send 5 canoes down the river — each canoe a glyph, its HEADING the vowel: `>` o (ᐳ) · `^` i (ᐱ) · `<` a (ᐸ) · `v` ê (ᐯ).
A message = a heading-sequence: `>>>>>` · `>>>^>` · `>^>^>` · `<<^^<`. You TURN the canoes, you don't add them →
**4⁵ = 1,024** formations from 5 positions; the length-dot doubles each axis → 8⁵ = **32,768**. 6 canoes = 4,096;
10 = ~1 million. Exponential — "it gets pretty wild."
**And each formation is a MELODY** — heading→vowel→pitch is `[PROVEN]` in `syllabic_to_event.rs:19‑24`:
`v`ê→C4 · `^`i→E4 · `>`o→G4 · `<`a→A4. So `>>>>>` = G-G-G-G-G (drone); `<<^^<` = A-A-E-E-A (a contour).
The flotilla you SEE is the song you HEAR; `drum_circle` is the paddle-cadence. One river carries both.

### THREE LAYERS + THE RIVER BED — Sean, 2026-07-05 (naming ruling + take-out key)
**Do NOT lead with cremantics.** It is a DIFFERENT thing than Cree syllabics and calligraphy — never conflate the three:
- **water** = the flow of data (public, observable). We START with the flow.
- **canoes** = **Cree syllabics / calligraphy** — the marks riding the flow, the visible language + craft. **THE PEOPLE'S. This leads.** `[SOURCE-grounded]`
- **river bed** = **cremantics** — the shaped 5-axis channel underneath; decides where the water runs and WHERE YOU PULL THE CANOE OFF. Ours, hidden, never the headline.

**Take-out points = the key** ("only we know where to pull the canoe off"): the flow can be watched; the landings cannot. Meaning is not hidden in the current — it is hidden in *knowing where to leave it*. **Security by landing-secrecy, not flow-secrecy.**

RULING: lead with the river + syllabics; cremantics stays the bed beneath. **cremantics ≠ Cree syllabics ≠ forge-calligraphy.** The 5-axis map (above) IS the bed.
