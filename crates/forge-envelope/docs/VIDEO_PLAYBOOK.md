# VIDEO PLAYBOOK — Forge V3 Demo Shell (revised 2026-08-26)

Supersedes the 2026-08-20 bridge-audit playbook (atticked:
`.forge/audits/2026-08-26-demo-redline/attic-VIDEO_PLAYBOOK-pre-2026-08-26.md`).
The recorded surface is `crates/studio-tauri` — the Tauri v2 5D-sky shell.
Canonical storyboard: FINAL-SUBMISSION-DOSSIER §7. Rules floor: ≤4:00 total
(only the first 4 minutes are judged) · public YouTube or Vimeo · English or EN subs.

## Pre-flight (before hitting record)
1. Boot the shell once fully: open the terminal dock (first OPEN spawns ConPTY,
   ~500 ms), click one deep star (warms the sky VBO), then relaunch clean.
2. Footer pills: DAEMON :13013 green. S13 GEMMA reads OFFLINE unless the sidecar
   is up — boot it, or narrate the local-first fallback on camera.
3. The CYOA slip appears ~5 s after the rite arrival line — hold the frame.
4. Do not claim on camera: full S13-accelerated generation (the lm_head is not
   yet dosed — `output_weight.s13m` absent from the seat). Persistence and the
   Toll-Gate arc ARE live: choices save under `.forge/mud/` and the arc ends at
   the TOLL GATE label.

## Shot list (~3:50)
| Time | Beat |
| :--- | :--- |
| 0:00–0:25 | Cold open: 5D sky drag-glide, Milky Way dome, Sol bloom/godrays. One-liner: 9B Gemma in 1.72 GB, all local. |
| 0:25–0:50 | Free flight; double-strike a deep star → sail; star dossier glass (spectral badge, OBAFGKM strip, live oscilloscope). |
| 0:50–1:20 | Astrolabe slider raises the brass; fly into Sol for immersion. |
| 1:20–2:10 | MUD button → Thirteen Tolls rite → natal star → player sheet → CYOA choices → TOLL GATE arrival; show the `.forge/mud/` save files. |
| 2:10–2:40 | Open ConPTY dock (gemini in the glass); cut to terminal: airgap red/green run — 5/5 blocked. |
| 2:40–3:20 | Terminal receipts: `cargo test --workspace` (5,016 green) · bench highlights (2.0 Gweights/s CPU, 21–23 tok/s real-9B, 269 tok/s roofline) · code-merge run (327 files → 76.25 ms, Merkle match). |
| 3:20–3:50 | Pull back to full sky; close card: repo + DOI + 2748684 Alberta LTD. |

## Capture notes
- 2560×1440 or the shell's native window, 60 fps; molten palette reads best on a
  dark room profile.
- `forgewright.exe capture "Forge V3 Demo Shell" <out.png>` for stills;
  screen-record the window for motion.
- After upload: paste the public link into docs/SUBMISSION_ENTRY.md ("Demo")
  and docs/DEVPOST.md ("Try it out") in the clean-room repo.
