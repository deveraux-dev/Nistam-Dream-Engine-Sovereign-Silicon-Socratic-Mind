[package]
name = "forge-input-v3"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Deterministic gamepad quantization — irregular XInput polls to uniform 240Hz integer frames, byte-replayable."

# Zero dependencies. Ported from F:\NewRepo\crates\forge-input (also zero
# runtime deps); the source's `proptest` dev-dependency is not ported —
# the fixed-value unit tests it duplicated as properties are kept instead.
[dependencies]

[lints]
workspace = true
