# scc -- Sovereign Creation Compiler, ported in full (all 8 modules: contract,
# governance, drain, evidence, poc, roadmap, buff, wgsl) from
# F:\NewRepo\crates\scc (2026-08-15) -- source-compiler ladder Owner: wgsl (the
# only module that ladder actually names), plus the rest of the crate at the
# Architect's explicit direction ("bring it all", "(all 8 files").
[package]
name = "forge-scc-v3"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
forge-core-v3 = { path = "../forge-core-v3" }

[lints]
workspace = true
