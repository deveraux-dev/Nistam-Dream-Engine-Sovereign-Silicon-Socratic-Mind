// Fourth worked example (2026-08-22 fan-out proof): the `Verbatim` payload
// kind — the whole payload is one raw string field. Not wired into live
// dispatch.
(
    name: "example_verbatim",
    payload: Verbatim,
    mutating: false,
)
