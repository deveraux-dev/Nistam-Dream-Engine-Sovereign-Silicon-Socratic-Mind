[package]
name = "forge-ttl-v3"
version = "0.1.0"
edition = "2021"

[dependencies]
zeroize = { version = "1.7", features = ["zeroize_derive"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
