[package]
name = "pp-math-v3"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Pure physics equations — stateless functions and ODE/PDE right-hand sides. No state, no integrators, no opinion on precision. Ported verbatim from F:\\NewRepo\\crates\\pp-math."

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[lints]
workspace = true
